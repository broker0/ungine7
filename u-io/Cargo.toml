[package]
name = "u-io"

version = "0.1.0"
edition = "2024"
publish.workspace = true

[features]
serde = ["dep:serde"]

[dependencies]
u-core = { path = "../u-core" }
bytes = "1"
serde = { version = "1", features = ["derive"], optional = true }
