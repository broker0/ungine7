-- constants/hues.lua — Color / hue constants.
--
-- Usage:  broadcast:speech(serial, graphic, words, { color = HUE.SPELL_WORDS })

HUE = {
    SPELL_WORDS   = 0x03E8,
    HEAL_FEEDBACK = 0x0039,
    SYSTEM_RED    = 0x0025,
}
